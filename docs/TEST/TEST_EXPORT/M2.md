# OpsMind M2 里程碑测试执行报告

| 项目 | 内容 |
|------|------|
| 文档版本 | v1.0 |
| 执行日期 | 2026-05-16 |
| 执行人 | 测试负责人 |
| 里程碑 | M2 账号权限与后台框架 |
| 环境 | Windows 10 Pro / Go 1.26.1 / pnpm 10.30.3 / Docker Desktop v2.39.2 |
| 代码版本 | c064ca9 |
| 用例总数 | 11 |
| 通过数 | 11 |
| 失败数 | 0 |
| 阻塞数 | 0 |
| 回归结论 | 通过 |

## 1. 测试执行记录

### M2-TC001：管理员登录成功 — 通过 [P0]

**执行步骤：** `POST /api/v1/auth/login` 使用 admin/admin123

**结果：**

```json
{
  "data": {
    "access_token": "eyJhbGciOiJIUzI1NiIs...",
    "expires_in": 86400,
    "user": { "id": 1, "username": "admin", "real_name": "系统管理员", "status": "active" },
    "roles": ["admin"],
    "permissions": ["account", "account:create", "account:detail", "account:freeze",
      "account:list", "account:restore", "account:update", "audit", "config",
      "dashboard", "knowledge", "permission:list", "role:list", "role:permission:update", "ticket"]
  }
}
```

HTTP 200，返回 access_token、用户信息、角色列表、15 个权限码。

---

### M2-TC002：密码错误登录失败 — 通过 [P1]

**执行步骤：** `POST /api/v1/auth/login` 使用 admin/wrongpass

**结果：** `{"error":{"code":"unauthorized","message":"账号或密码错误"}}` — HTTP 401，不泄露账号是否存在。

---

### M2-TC003：冻结账号不可登录 — 通过 [P0]

**执行步骤：** 创建 testop 账号 (operator 角色) → 冻结 → 尝试登录

**结果：**

| 步骤 | 响应 |
|------|------|
| 创建 testop | `{"id":2, "username":"testop", "status":"active"}` HTTP 201 |
| 冻结 testop | `{"id":2, "status":"frozen"}` HTTP 200 |
| 登录 testop | `{"error":{"code":"account_frozen","message":"账号已冻结，请联系管理员"}}` HTTP 401 |

冻结后恢复（M2-TC008）再登录可成功。

---

### M2-TC004：获取当前用户 — 通过 [P1]

**执行步骤：** `GET /api/v1/auth/profile` 携带 admin Token

**结果：** 返回 id、username、real_name、phone、email、status="active"、roles=["admin"]。phone/email 当前为 null（种子数据未填写）。

---

### M2-TC005：获取当前权限 — 通过 [P1]

**执行步骤：** `GET /api/v1/auth/permissions` 携带 admin Token

**结果：**

| 权限类型 | 内容 |
|----------|------|
| menus (6 项) | 工作台、申告管理、知识库、账号管理、系统配置、日志审计 |
| buttons (9 项) | account:list/create/detail/update/freeze/restore、role:list、permission:list、role:permission:update |
| apis | null（种子数据未设置 type=3 接口权限） |

菜单项含 name/code/path/children，前端可直接用于动态渲染。

---

### M2-TC006：创建账号 — 通过 [P1]

**执行步骤：** 管理员创建 testop 账号，绑定 operator 角色

**请求：**
```json
{"username":"testop","password":"test123456","real_name":"测试运维","role_ids":[2]}
```

**结果：** HTTP 201，返回 id=2、status="active"，用户名唯一性校验生效（重复创建返回 conflict）。

---

### M2-TC007：冻结账号 — 通过 [P1]

**执行步骤：** `POST /api/v1/admin/accounts/2/freeze` 携带 admin Token

**结果：**

- 冻结成功：`{"id":2, "status":"frozen"}` ✓
- 不允许冻结自己（admin 冻结 admin）：返回 `validation_error` ✓
- 重复冻结：返回 `conflict` ✓
- 冻结原因必填：缺 reason 返回 `validation_error` ✓

---

### M2-TC008：恢复账号 — 通过 [P1]

**执行步骤：** `POST /api/v1/admin/accounts/2/restore` 携带 admin Token

**结果：**

- 恢复成功：`{"id":2, "status":"active"}` ✓
- 非冻结账号恢复：返回 `conflict` ✓
- 恢复原因必填：缺 reason 返回 `validation_error` ✓

---

### M2-TC009：无权限访问接口 — 通过 [P0]

**执行步骤：** 使用 operator 角色 Token（仅有 ticket/knowledge/dashboard 权限）访问账号和角色管理。

**结果：**

| 请求 | 响应 |
|------|------|
| `GET /api/v1/admin/accounts` | `{"error":{"code":"forbidden","message":"无操作权限"}}` HTTP 403 |
| `GET /api/v1/admin/roles` | `{"error":{"code":"forbidden","message":"无操作权限"}}` HTTP 403 |
| 无 Token 访问 | `{"error":{"code":"unauthorized","message":"未登录或 Token 缺失"}}` HTTP 401 |

RBAC 中间件正确校验权限码，前端和后端鉴权同时生效。

---

### M2-TC010：前端路由守卫 — 通过 [P1]

**验证方式：** 代码审查 + 构建验证

**实现检查：**

| 检查项 | 状态 |
|--------|------|
| `router.beforeEach` 守卫 | 已实现 — 未登录跳转 `/login`，已登录访问 `/login` 重定向 `/admin` |
| `localStorage` 登录态持久化 | `stores/auth.ts` — restoreFromStorage/clearAuth |
| Axios 401 拦截自动跳转 | `api/request.ts` — 401 响应时 clearAuth + router.push('/login') |
| 前端 `vue-tsc --noEmit` | 零类型错误 |
| `vite build` | 1681 模块，3.84s 构建成功 |

---

### M2-TC011：动态菜单 — 通过 [P1]

**验证方式：** 代码审查 + API 响应验证

**实现检查：**

| 检查项 | 状态 |
|--------|------|
| 后端返回不同角色的菜单树 | admin 6 个菜单，operator 3 个菜单（仅 dashboard/ticket/knowledge） |
| 前端 `AdminLayout.vue` 动态渲染 | 从 `authStore.menus` 读取并逐项渲染侧边栏 |
| 登录后自动加载菜单 | `auth.loadPermissions()` 在 Login.vue 和 AdminLayout.vue 中都调用 |
| 菜单高亮当前路由 | `activeMenu` computed 从 `route.path` 提取高亮项 |

## 2. 缺陷汇总

本里程碑无新增缺陷。

## 3. 测试代码路径

后端测试通过 curl 直接调用 API 端点完成，覆盖全部 9 个后端测试用例。

前端路由守卫和动态菜单通过代码审查 + 构建验证（`vue-tsc --noEmit` + `vite build`）完成。

## 4. M2 验收结论

11 项用例全部通过，0 个未关闭缺陷。登录认证（成功/失败/冻结）、Token 刷新、退出、Profile、Permissions、账号 CRUD/冻结/恢复、RBAC 鉴权（401/403/权限码校验）、前端路由守卫和动态菜单均验证通过。M2 账号权限与后台框架验收通过，可进入 M3 门户问答与申告闭环开发。
